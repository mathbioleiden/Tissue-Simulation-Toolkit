#pragma once

/**
 * @brief Compute random variable of erlang distribution
 * @param k number of modes
 * @param lambda lambda variable
 * @return Random variable from erlang distribution
 */
double erlang(int k, double lambda);